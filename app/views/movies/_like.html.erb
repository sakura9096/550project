<%= form_for(current_user.movie_relations.build) do |f| %>
  <div><%= hidden_field_tag :movie_id, movie.id %></div>
  <%= f.submit "Like! | #{movie.users.count}", class: "btn btn-primary" %>
<% end %>