<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Movie Social Network", root_path, class: "navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <%= form_for :search_results, url: :search_results, method: :get, html:{class: "navbar-form navbar-left"} do |f| %>
            <%= text_field_tag :keywords, nil,
                       placeholder: "Search", class: "form-control" %> 
            <%= submit_tag "Submit", class: "btn btn-primary" %>
        <% end %>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "Movies", genres_path  %></a></li>
        <li><%= link_to "Users", users_path  %></a></li>
        <% if user_signed_in? %>
          <%
            badge_class = ""
            badge_class = "new" if current_user.notifications.unread.count > 0
          %> 
          <li><%= link_to "Quiz", surveys_path  %></li>
          <li class="notification-count">
            <a href="<%= notifications_path %>" class=<%= badge_class %> > Notifications <span class="count"><%= current_user.notifications.unread.count %></span></a>
          </li> 
          <li><%= link_to "Account Settings", edit_user_registration_path %></li>
          <li><%= link_to "Log out", destroy_user_session_path, method: :delete %></li>
        <% else %>
          <li><%= link_to "Sign in", new_user_session_path %></li>
        <% end %>

      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
